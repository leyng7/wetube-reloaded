extends base

block content
  div#videoContainer(data-id=video._id).row
    video(autoplay=true, controls=true).col-md-12
      source(src="/" + video.fileUrl, type="video/mp4")
block scripts
  // script(src="/static/js/videoPlayer.js")
