mixin message(kind, text)
  .toast.text-white(role="alert" aria-live="assertive" aria-atomic="true" class=`bg-${kind}`)
    .d-flex
      .toast-body=text
      button.btn-close.btn-close-white.me-2.m-auto(type="button", data-bs-dismiss="toast", aria-label="Close")
users/edit
